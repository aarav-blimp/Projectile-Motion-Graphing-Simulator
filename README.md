# Projectile-Motion-Graphing-Simulator
It is a simple python program which uses the libraries matplotlib and numpy to graph and calculate the trajectory of an object using the laws of projectile motion. The setup only requires you to have python installed
